# .impeccable.md — aether-nav 设计上下文

> 由 impeccable 技能在 2026-09-14 首轮审计时建立。
> 内容取自 **用户自撰的 PRD**（`PRD-个人导航页面-v1.0.md` §1–§5）与已确认的 Stitch 原型稿，
> **不是**从代码反推的。如果与实际意图不符，请直接改本文件。

## 1. 目标用户（PRD §2.1）

| 角色 | 描述 | 核心诉求 |
|------|------|----------|
| 管理员（Owner） | 自建服务器 / 群晖的技术玩家 | 完全掌控：链接、分类、用户、权限、图标 |
| 普通成员 | 家庭成员 / 小团队成员 | 快速找到常用服务；可管理自己添加的链接 |
| 访客（只读） | 临时外部人员 | 仅见权限为「所有人」的链接 + 搜索引擎 |

## 2. 使用场景（PRD §2.2）

- A 搜索引擎直达：首页 Hero 输入 → 站内过滤 / 外部引擎搜索
- B 浏览发现：侧边栏选分类 → 主区卡片列表 → 点击进入
- C 管理员维护：左下角「管理后台」→ 增删改链接/分类、配置权限
- D 内外网切换：右上角切模式 → 卡片 URL 自动适配
- E 密码链接：点击设密链接 → 弹密码框 → 验证后打开
- F 访客使用：免登 → 仅见「所有人」链接
- G 快速添加：任意页「+ 快速添加」→ 填 URL + 选分类

**使用频次特征**：日常高频、目的性极强的工具型使用（找入口 → 点进去），
不是内容消费场景。**首屏到点击的路径越短越好**。

## 3. 品牌性格 / 语气（PRD §5.1）

> 现代仪表盘 / 卡片式布局 / 浅色友好 / 紫罗兰主调 / 圆角柔和 / 轻量阴影
>
> 整体气质：干净、轻快、不厚重 —— 偏向**消费级产品感**，而非传统 B 端后台的冷硬工具风。

设计取向**已确定**（源自参考设计稿）。后续优化应在**既有体系内一致化**，
不要另起炉灶换风格；需要突破时先与用户确认。

## 4. 设计系统基线

- **色彩**：`--c-*` 令牌体系（RGB 通道三元组），默认主色 `83 65 205`（#5341CD，紫罗兰）
  + **6 套可选配色** `[data-palette]`（macaron / sunset / mint / cosmic / berry）挂载在 `<html>`
- **主题**：浅色 / 暗色 / 跟随系统（`html.dark` 类切换，令牌整体翻转）
- **排版**：`tailwind.config.js` 的 fontSize 元组（headline-lg/md/sm、body-md/sm、label-sm），
  字距随字号（大字负字距、小标签正字距）；拉丁/数字 Inter，中文回落 PingFang SC / 微软雅黑
- **圆角**：4 / 8 / 12 / 16 / 20 / full
- **阴影**：宁轻勿重，Level 0–4；**强调色阴影必须从 `--c-primary` 派生**（见下）
- **间距**：4px 基准栅格
- **动效**：令牌化 —— `--ease-spring` `cubic-bezier(.32,.72,0,1)`（临界阻尼，**无回弹**）、
  `--ease-out`；`--dur-fast 150ms` / `--dur-base 180ms` / `--dur-slow 220ms` / `--dur-panel 300ms`
- **图标**：Material Symbols（FILL 轴切实心/描边）+ Feather / Boxicons / FontAwesome / Flaticon
- **移动端（<768px）**：**双份模板**（卡片 + 桌面表格），断点一律 `md:`；PC 零改动
- **无障碍**：`prefers-reduced-motion` / `prefers-reduced-transparency` / `prefers-contrast: more`
  三信号 + 全局 `:focus-visible` 3px

## 5. 硬约束（改代码前必读）

- **未获用户明确指令，不得 commit / push**
- 任何**强调色**（阴影、光晕、图表主色）必须从 `--c-primary` 派生，不得写死色值
- 任何**文字/网格色**必须从 `--c-on-surface(-variant)` / `--c-outline(-variant)` 派生，
  否则暗色主题下会不可见
- 立体/悬浮效果只在 `@media (hover: hover) and (pointer: fine)` 内生效（触屏避免粘滞）
- 改同一文件的多处，**一条消息只发一个 Edit**（并发 Edit 会丢改动）
- 构建：`frontend/` 下 `node node_modules/vite/bin/vite.js build`，断言 `VITE_EXIT=0`

## 6. 已知未决 / 待用户拍板

- 图表 `PALETTE` 其余 9 个分类色仍是固定色板（Tailwind 默认色相），是否按配色重算
- `Inter` 为 PRD 指定字体，但从「避免通用 AI 审美」角度看属于默认选择；
  是否引入更具辨识度的展示字体（中文仍需系统字体兜底）
- `backdrop-filter` 共 33 处（内容卡片也用了毛玻璃）；移动端滚动时模糊开销较大，
  是否收敛为「仅固定层（顶栏/侧栏/弹窗）用玻璃，内容卡片改实心」
